{
  "name": "firebase",
  "version": "1.0.0",
  "description": "Firebase emulator seed scripts for pomodoro-timer-sync",
  "main": "index.js",
  "scripts": {
    "seed": "node seed/seed-emulator.js",
    "test": "firebase emulators:exec --only auth,firestore \"jest --forceExit\"",
    "test:watch": "jest --watch"
  },
  "jest": {
    "testEnvironment": "node",
    "testTimeout": 15000
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "firebase-admin": "^13.8.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "jest": "^30.3.0"
  }
}
